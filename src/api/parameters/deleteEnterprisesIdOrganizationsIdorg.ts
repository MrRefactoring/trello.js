export interface DeleteEnterprisesIdOrganizationsIdorg {
  /** ID of the enterprise to retrieve. */
  id: Record<string, any>;
  /** ID of the organization to be removed from the enterprise. */
  idOrg: Record<string, any>;
}
