export interface GetEnterpriseAdmins {
  /** ID of the enterprise to retrieve. */
  id: Record<string, any>;
  /** Any valid value that the [nested member field resource]() accepts. */
  fields?: string;
}
