export interface CreateLabel {
  /** Name for the label */
  name: string;
  /** The color for the label. */
  color: string;
  /** The ID of the Board to create the Label on. */
  idBoard: string;
}
