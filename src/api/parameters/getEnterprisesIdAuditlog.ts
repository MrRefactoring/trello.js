export interface GetEnterprisesIdAuditlog {
  /** The API key to use */
  key: Record<string, any>;
  /** The API token to use. Note: This must be an Enterprise admin's token. */
  token: Record<string, any>;
}
